import projects from "../../canon/projects.json";
import relations from "../../canon/relations.json";
import { ArchivePage, ArchiveSection } from "../archive-page";

export default function FamiliesPage() { const groups = Array.from(new Set(projects.map((project) => project.family))); return <ArchivePage kicker="03 / FAMÍLIAS E RELAÇÕES" title="As coisas conversam entre si" intro="Família aqui não quer dizer que tudo é o mesmo projeto. É só uma forma de enxergar vizinhanças, componentes, paralelos e limites sem apagar as diferenças."><ArchiveSection label="OVERVIEW_" title="Prateleiras do arquivo"><div className="family-shelves">{groups.map((group) => <section key={group}><span className="eyebrow">{group}</span><strong>{projects.filter((project) => project.family === group).length}</strong><div>{projects.filter((project) => project.family === group).map((project) => <a href={`/projects/${project.id}`} key={project.id}>{project.name} ↗</a>)}</div></section>)}</div></ArchiveSection><ArchiveSection label="FULL GRAPH_" title="Relações com cuidado"><div className="relation-list">{relations.map((relation) => <div key={`${relation.from}-${relation.to}-${relation.type}`}><b>{relation.type}</b><span>{relation.description} <small>{relation.confidence}</small></span></div>)}</div></ArchiveSection></ArchivePage>; }
