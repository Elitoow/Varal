import projects from "../../canon/projects.json";
import { ArchivePage, ArchiveSection } from "../archive-page";

export default function GraveyardPage() { const entries = projects.filter((project) => project.state !== "FROZEN" || project.build === "NO" || project.runtime === "NO" || project.maturity.includes("LOST") || project.maturity.includes("MISSING")); return <ArchivePage kicker="05 / CEMITÉRIO" title="O que não chegou até aqui" intro="O Graveyard não é uma lista de fracassos. É a sala dos finais incompletos: builds perdidos, source interrompido, hardware que nunca virou placa e perguntas que o arquivo ainda não consegue responder."><ArchiveSection label="ENTRADAS" title="Cada ausência tem uma forma"><div className="graveyard-list">{entries.map((project) => <a href={`/projects/${project.id}`} key={project.id}><span className="eyebrow">{project.state}</span><h3>{project.name}</h3><p>{project.reality}</p><strong>Ver o que sobreviveu ↗</strong></a>)}</div></ArchiveSection><ArchiveSection label="REGRA" title="Perdido não significa nunca existiu"><p>O arquivo registra o limite da recuperação. Uma ausência pode ser um build não preservado, uma origem não encontrada ou uma execução que não foi comprovada. Nenhuma dessas coisas deve ser preenchida por imaginação.</p></ArchiveSection></ArchivePage>; }
