import timeline from "../../canon/timeline.json";
import { ArchivePage, ArchiveSection } from "../archive-page";

export default function ArchaeologyPage() { return <ArchivePage kicker="08 / ARCHAEOLOGY 001—069" title="Como fomos procurar" intro="Algumas coisas foram lembradas de um jeito e encontradas de outro. A campanha arqueológica existe para fazer essa diferença aparecer, sem transformar incerteza em narrativa limpa."><ArchiveSection label="A CAMPANHA" title="69 investigações, um congelamento"><p>Os relatórios percorrem origens, versões, máquinas, jogos, modelos e arquivos. O Master Index 069 congela a contagem em 20 projetos canônicos; a apresentação preserva entidades adicionais quando elas têm importância arqueológica.</p></ArchiveSection><ArchiveSection label="MARCOS PRESERVADOS" title="Uma linha do tempo com categoria"><div className="timeline">{timeline.map((event) => <div className="timeline-row" key={`${event.kind}-${event.label}`}><span className="timeline-date">{event.date}</span><span className="timeline-dot" /><div><strong>{event.kind}</strong><p>{event.label}</p><small>{event.confidence} confiança</small></div></div>)}</div></ArchiveSection><ArchiveSection label="PRINCÍPIOS" title="Fonte antes da história"><div className="principle-grid"><span>SOURCE ≠ RUNTIME</span><span>BUILD ≠ TESTED BUILD</span><span>MODEL ≠ PERSON</span><span>CAD ≠ HARDWARE</span><span>MISSING ≠ NEVER EXISTED</span><span>UNKNOWN ≠ FALSE</span></div></ArchiveSection></ArchivePage>; }
